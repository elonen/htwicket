# Build outputs & local caches — never an input.
target/
dist_deb/
.local-tmp/

# VCS / CI / editor / tooling metadata.
.git/
.github/
.gitignore
.dockerignore
**/.DS_Store
po/*.pot

# Demo runs stock images with bind mounts — nothing in demo/ is a build input.
demo/
