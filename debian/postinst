#!/bin/sh
set -e

if [ "$1" = "configure" ] && [ -z "$2" ]; then
    echo "htwicket: edit /etc/htwicket.toml and add a user, then start with:" >&2
    echo "          systemctl enable --now htwicket" >&2
fi

#DEBHELPER#
